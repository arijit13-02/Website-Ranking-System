import requests,webbrowser
import os
import os.path
import sys
import time
from bs4 import BeautifulSoup
from tkinter import *
struct=Tk()
struct.geometry("450x200")
struct.title("WRS")
label=Label(struct,text="This part of the website \nis still in progress! Stay Tuned!\n^(*_*)^",bg="teal",fg="white",font=("Times",20,"bold"))
label.place(x=40,y=40)
struct.config(background="teal")

struct.mainloop()

